/* This shouldn't need to be distributed but I thought it would be fun to build some solver info
 */
export {
  buildMetadata,
  scoreWord,
  scoreWords,
  simpleSolve,
  testGetWords,
  getInfoFromGuess,
  getInfoFromResult,
} from "./basic";
export { fancySolve, fancyHardSolve } from "./fancy";
